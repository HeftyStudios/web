{
  "name": "arena-game-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:icons": "node scripts/sync-arena-icons.mjs",
    "dev": "npm run prepare:icons && ASTRO_TELEMETRY_DISABLED=1 astro dev",
    "build": "npm run prepare:icons && ASTRO_TELEMETRY_DISABLED=1 astro check && ASTRO_TELEMETRY_DISABLED=1 astro build",
    "preview": "npm run prepare:icons && ASTRO_TELEMETRY_DISABLED=1 astro preview",
    "check": "ASTRO_TELEMETRY_DISABLED=1 astro check"
  },
  "dependencies": {
    "@hefty-studios/arena-game-data": "^2.3.2",
    "astro": "^5.10.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@types/node": "^24.0.1",
    "typescript": "^5.8.3"
  }
}
